<div class="row">
    <div class="page-header">
        <h1 class="page-title">{{ $title }}</h1>
        @if ($description)
            <h5 class="page-description">{{ $description }}</h5>
        @endif
    </div>
</div>
